import { Platform } from 'react-native';

export async function requestUserPermission() {
    // Firebase messaging is stubbed out for now until full Android setup in the next phase.
    return false;
}
